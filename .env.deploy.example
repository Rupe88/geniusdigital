# Copy to .env.deploy on the droplet (chmod 600). CI usually writes this file each deploy.
#
# Pre-built image from GHCR. For **cron pull** deploys, use the **:latest** tag (CI updates it every push).
# FRONTEND_IMAGE=ghcr.io/owner/geniusdigital:latest
# For immutable tags per commit (optional / SSH deploy): ghcr.io/owner/geniusdigital:<git-sha>

# Public HTTPS API base (must match backend routes, usually ends in /api)
NEXT_PUBLIC_API_URL=https://api.geniusdigi.com/api

# Netlify + HTTP-only droplet (avoids mixed content): proxy via Next — see next.config.ts rewrites
# BACKEND_HTTP_ORIGIN=http://YOUR_DROPLET_IP
# NEXT_PUBLIC_API_URL=https://YOUR_SITE.netlify.app/backend-proxy/api

# Public site URL (SEO, redirects, backend FRONTEND_URL / CORS alignment)
NEXT_PUBLIC_APP_URL=https://geniusshiksha.com
